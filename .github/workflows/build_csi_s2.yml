name: Build Flipper WiFi Dev Board S2 + CSI

# Builds ESP32 Marauder for the ESP32-S2 Flipper WiFi Dev Board (MARAUDER_FLIPPER)
# WITH the CSI Sense integration (UART transport; no BLE on S2). Same proven recipe
# as build_parallel, trimmed to the S2 target.

on:
  workflow_dispatch:

jobs:
  compile_sketch:
    name: build ${{ matrix.board.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        board:
          - { name: "Flipper Zero WiFi Dev Board", flag: "MARAUDER_FLIPPER", fbqn: "esp32:esp32:esp32s2:PartitionScheme=min_spiffs,FlashSize=4M,PSRAM=enabled", file_name: "flipper_csi", tft: false, tft_file: "", build_dir: "esp32s2", addr: "0x1000", idf_ver: "2.0.11", nimble_ver: "1.3.8" }

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Install Arduino CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | sh
          echo "/home/runner/work/ESP32Marauder/ESP32Marauder/bin" >> $GITHUB_PATH
          export PATH=$PATH:/home/runner/work/ESP32Marauder/ESP32Marauder/bin
          arduino-cli version

      - name: Build TestFile with ESP32 v${{ matrix.board.idf_ver }}
        uses: ArminJo/arduino-test-compile@v3.2.1
        with:
          sketch-names: TestFile.ino
          arduino-board-fqbn: esp32:esp32:esp32s2
          arduino-platform: esp32:esp32@${{ matrix.board.idf_ver }}
          platform-url: https://github.com/espressif/arduino-esp32/releases/download/${{ matrix.board.idf_ver }}/package_esp32_dev_index.json

      - name: Install ESP32Ping
        uses: actions/checkout@v4
        with: { repository: marian-craciunescu/ESP32Ping, ref: "1.6", path: CustomESP32Ping }
      - name: Install AsyncTCP
        uses: actions/checkout@v4
        with: { repository: ESP32Async/AsyncTCP, ref: "v3.4.8", path: CustomAsyncTCP }
      - name: Install MicroNMEA
        uses: actions/checkout@v4
        with: { repository: stevemarple/MicroNMEA, ref: "v2.0.6", path: CustomMicroNMEA }
      - name: Install ESPAsyncWebServer
        uses: actions/checkout@v4
        with: { repository: ESP32Async/ESPAsyncWebServer, ref: "v3.8.1", path: CustomESPAsyncWebServer }
      - name: Install TFT_eSPI
        uses: actions/checkout@v4
        with: { repository: Bodmer/TFT_eSPI, ref: "V2.5.34", path: CustomTFT_eSPI }
      - name: Install XPT2046_Touchscreen
        uses: actions/checkout@v4
        with: { repository: PaulStoffregen/XPT2046_Touchscreen, ref: "v1.4", path: CustomXPT2046_Touchscreen }
      - name: Install lv_arduino
        uses: actions/checkout@v4
        with: { repository: lvgl/lv_arduino, ref: "3.0.0", path: Customlv_arduino }
      - name: Install JPEGDecoder
        uses: actions/checkout@v4
        with: { repository: Bodmer/JPEGDecoder, ref: "1.8.0", path: CustomJPEGDecoder }
      - name: Install NimBLE-Arduino
        uses: actions/checkout@v4
        with: { repository: h2zero/NimBLE-Arduino, ref: "${{ matrix.board.nimble_ver }}", path: CustomNimBLE-Arduino }
      - name: Install Adafruit_NeoPixel
        uses: actions/checkout@v4
        with: { repository: adafruit/Adafruit_NeoPixel, ref: "1.12.0", path: CustomAdafruit_NeoPixel }
      - name: Install ArduinoJson
        uses: actions/checkout@v4
        with: { repository: bblanchon/ArduinoJson, ref: "v6.18.2", path: CustomArduinoJson }
      - name: Install LinkedList
        uses: actions/checkout@v4
        with: { repository: ivanseidel/LinkedList, ref: "v1.3.3", path: CustomLinkedList }
      - name: Install EspSoftwareSerial
        uses: actions/checkout@v4
        with: { repository: plerup/espsoftwareserial, ref: "8.1.0", path: CustomEspSoftwareSerial }
      - name: Install Adafruit_BusIO
        uses: actions/checkout@v4
        with: { repository: adafruit/Adafruit_BusIO, ref: "1.15.0", path: CustomAdafruit_BusIO }
      - name: Install Adafruit_MAX1704X
        uses: actions/checkout@v4
        with: { repository: adafruit/Adafruit_MAX1704X, ref: "1.0.2", path: CustomAdafruit_MAX1704X }
      - name: Install Adafruit_TCA8418
        run: cp -r libraries/Adafruit_TCA8418 CustomAdafruit_TCA8418

      - name: Configure TFT_eSPI
        run: |
          rm -f CustomTFT_eSPI/User_Setup_Select.h
          cp User*.h CustomTFT_eSPI/

      - name: Install Esptool
        run: pip install esptool

      - name: Modify platform.txt
        run: |
          for i in $(find /home/runner/.arduino15/packages/esp32/hardware/esp32/ -name "platform.txt"); do
            sed -i 's/compiler.c.elf.libs.esp32s2=/compiler.c.elf.libs.esp32s2=-zmuldefs /' "$i"
          done

      - name: Build Marauder + CSI for ${{ matrix.board.name }}
        uses: ArminJo/arduino-test-compile@v3.3.0
        with:
          sketch-names: esp32_marauder.ino
          arduino-board-fqbn: ${{ matrix.board.fbqn }}
          extra-arduino-cli-args: "--warnings none --build-property compiler.cpp.extra_flags='-D${{ matrix.board.flag }}'"
          arduino-platform: esp32:esp32@${{ matrix.board.idf_ver }}
          platform-url: https://github.com/espressif/arduino-esp32/releases/download/${{ matrix.board.idf_ver }}/package_esp32_dev_index.json

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: marauder-csi-s2
          path: ./esp32_marauder/build/esp32.esp32.${{ matrix.board.build_dir }}/esp32_marauder.ino.bin
          retention-days: 5
